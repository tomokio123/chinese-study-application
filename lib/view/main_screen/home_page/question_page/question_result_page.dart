import 'package:flutter/material.dart';

class QuestionResultPage extends StatelessWidget {
  const QuestionResultPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
